{
  "scripts": {
    "dev": "cd ./frontend/ && yarn dev",
    "build": "cd ./frontend/ && yarn build",
    "go": "go run .",
    "start": "yarn build && yarn go",
    "watch": "nodemon"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
